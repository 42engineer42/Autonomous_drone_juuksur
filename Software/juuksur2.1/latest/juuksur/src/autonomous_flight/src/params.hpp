#pragma once

#include <float.h>
#include "ttmath.h"

extern float MAX_TRACK_VERT_DIST_DIF;
extern int MAX_SPEED;
extern int MAX_ANGLE_SPEED_DIF;
extern float MAX_TRACK_VERT_COS_ANGLE;
extern float MAX_DIRECTIONAL_SPEED;
extern int MAX_ANGLE_SPEED_DIF;
extern float MAX_OFFSET_FROM_TRACK;
extern float HORIZONTAL_FOV_RAD;
extern float VERTICAL_FOV_RAD;
extern int NATIVE_WIDTH;
extern int NATIVE_HEIGHT;
extern int DOWNSCALE_DIVIDER;
extern float FOCAL_LENGTH;
extern float PIXELS_PER_MM;
extern float X_ERROR_RANGE;
extern float Y_ERROR_RANGE;
extern float MIN_CONTOUR_AREA;
extern float MAX_CONTOUR_AREA;
extern V2 IMAGE_CENTER;
extern V3 UP_DIRECTION;

extern int PARTICLE_COUNT;
extern int RANDOM_PARTICLES;

extern float HVEL_KI;
extern float HVEL_KP;
extern int MAX_CONTROL_ROLL;
extern int MAX_CONTROL_PITCH;
extern float MAX_ACCELERATION;

extern V3 ROLL_AXIS;
extern V3 PITCH_AXIS;
